version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - echo "Installing dependencies..."
      - npm install

  pre_build:
    commands:
      - echo "Running pre-build checks..."
      - grep -Fq "Congratulations" index.html || echo "Warning: Congratulations not found in index.html"

  build:
    commands:
      - echo "Building application (if applicable)..."
      # build 단계에서 별도 빌드 과정 없으면 생략 가능

  post_build:
    commands:
      - echo "Zipping application for Elastic Beanstalk (Docker platform)..."
      - zip -r app.zip Dockerfile app.js index.html package.json commands.sh cron.yaml appspec.yml LICENSE.txt

artifacts:
  files:
    - app.zip
  discard-paths: yes
